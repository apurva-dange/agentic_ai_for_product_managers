# The Product Discovery Agent runs entirely on a deterministic local mock
# model by default and needs NO API key and NO values from this file.
#
# This variable is a placeholder for a possible future real-model adapter
# (see "Future improvements" in the README). It is not read by any code in
# this project today.
# ANTHROPIC_API_KEY=your-api-key-here
